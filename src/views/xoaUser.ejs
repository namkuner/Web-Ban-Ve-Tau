<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CRUD</title>
  <!-- Latest compiled and minified CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">

  <!-- jQuery library -->
  <script src="https://cdn.jsdelivr.net/npm/jquery@3.6.3/dist/jquery.slim.min.js"></script>

  <!-- Popper JS -->
  <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>

  <!-- Latest compiled JavaScript -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body>
  <div class="container">
    <div class="row">
      <div class="col-12 mt-3"><b>Delete User Infomation</b></div>
      <form action="/xoa-user" method="POST">
        <div class="row">
          <div class="col-md-6 mb-3">
            <label for="Ten">First Name</label>
            <input type="text" class="form-control" name="Ten" placeholder="Enter first name"
              value="<%= user.Ten %>" required readonly />
          </div>
          <div class="col-md-6 mb-3">
            <label for="Ho">Last Name</label>
            <input type="text" class="form-control" name="Ho" placeholder="Enter last name"
              value="<%=user.Ho%>" required readonly />
          </div>
        </div>

        <div class="mb-3">
          <label for="SDT">Phone Number</label>
          <input type="tel" class="form-control" name="SDT" placeholder="Enter phone number"
            value="<%=user.SDT%>" readonly />
        </div>

        <div class="mb-3">
          <label for="password">Password</label>
          <div class="input-group">
            <input type="password" class="form-control" name="password" placeholder="Enter password"
              value="<%=user.password%>" required readonly />
            <div class="input-group-append">
              <button class="btn btn-outline-secondary" type="button" onclick="togglePassword()">
                <i class="fa fa-eye"></i>
              </button>
            </div>
          </div>
        </div>



        <div class="mb-3">
          <label for="CMND">CMND</label>
          <input type="text" class="form-control" name="CMND" placeholder="Enter CMND" value="<%=user.CMND%>"
            readonly />
        </div>

        <div class="mb-3">
          <label for="DiaChi">DiaChi</label>
          <input type="text" class="form-control" name="DiaChi" placeholder="Enter DiaChi" value="<%=user.DiaChi%>"
            readonly />
        </div>


        <input type="hidden" name="id" value="<%=user.ID%>">
        <div class="col-12 mt-3"><b>Bạn có chắc chắn muốn xóa không</b></div>
        <button class="btn btn-primary" type="submit">Delete</button>
      </form>
      <script>
        function togglePassword() {
          var passwordInput = document.getElementsByName('password')[0];
          if (passwordInput.type === 'password') {
            passwordInput.type = 'text';
          } else {
            passwordInput.type = 'password';
          }
        }
      </script>
      <style>
        .container {
          max-width: 600px;
          margin: 0 auto;
        }

        form {
          padding: 20px;
          background-color: #f8f9fa;
          border-radius: 5px;
          box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.3);
        }

        input[type="text"],
        input[type="tel"],
        input[type="email"],
        input[type="password"],
        select {
          width: 100%;
          padding: 10px;
          margin-bottom: 20px;
          border-radius: 5px;
          border: none;
          background-color: #e9ecef;
          font-size: 16px;
        }

        input[type="submit"] {
          background-color: #007bff;
          color: #fff;
          padding: 10px 20px;
          border-radius: 5px;
          border: none;
          font-size: 16px;
          cursor: pointer;
        }

        input[type="submit"]:hover {
          background-color: #0069d9;
        }

        label {
          font-weight: bold;
          margin-bottom: 10px;
          display: block;
        }

        .col-md-6 {
          padding-right: 10px;
          padding-left: 10px;
        }

        @media (max-width: 767px) {
          .col-md-6 {
            margin-bottom: 10px;
          }
        }
      </style>
    </div>
  </div>
</body>

</html>